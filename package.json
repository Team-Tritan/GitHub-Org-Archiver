{
  "name": "github-archive",
  "module": "src/clone.ts",
  "type": "module",
  "scripts": {
    "build": " bun build --compile --outfile=./bin/clone ./src/clone.ts && chmod +x ./bin/clone",
    "dev": "bun run src/clone.ts",
    "start": "./bin/clone"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "chalk": "^5.4.1",
    "fs-extra": "^11.3.0",
    "p-limit": "^6.2.0",
    "simple-git": "^3.28.0"
  }
}
